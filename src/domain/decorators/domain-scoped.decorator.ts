import { ConcreteDependencyToken } from "@perivel/verdic";
import { Domain } from '../domain/domain';

/**
 * ContextScoped decorator
 * 
 * DomainScoped decorator makes a class automatically registered (lazy instanciated) to the domain object.
 * @param dependency The dependency key to use
 * 
 */

// export function DomainScoped<T>(dependency: ConcreteDependency<T>) {
//     return function(target: Function) {
        
//     }
// }
