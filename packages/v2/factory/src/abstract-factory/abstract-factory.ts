import { AbstractFactoryInterface } from "./abstract-factory.interface";

/**
 * AbstractFactory
 * 
 * AbstractFactory is an abstract factory.
 */

export abstract class AbstractFactory implements AbstractFactoryInterface {

    constructor() { }
}