{
  "name": "@domeniere/common",
  "version": "2.0.0",
  "description": "Common decorators and utilities for Domeniere applications.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "repository": "https://github.com/Perivel/domeniere",
  "author": "Patrick Luy <pluy@perivel.com>",
  "license": "MIT",
  "private": false,
  "scripts": {
    "build": "yarn clean && tsc",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "inspect:circular": "npx madge --circular --extensions ts ./",
    "lint": "tslint -c tslint.json 'src/**/*.ts'",
    "clean": "rimraf ./dist"
  },
  "dependencies": {
    "@domeniere/core": "^2.0.0",
    "@swindle/core": "^2.1.0",
    "typescript": "^4.4.2"
  },
  "devDependencies": {
    "@types/jest": "^26.0.20",
    "jest": "^26.6.3",
    "ts-jest": "^26.4.4",
    "rimraf": "^3.0.2"
  }
}
