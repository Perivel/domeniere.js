{
  "name": "@domeniere/scripts",
  "version": "1.0.0",
  "description": "Some scripts for managing the domeniere framework.",
  "keywords": [],
  "repository": "https://github.com/Perivel/domeniere",
  "author": "Patrick Luy <pluy@perivel.com>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage",
    "inspect:circular": "npx madge --circular --extensions ts ./",
    "lint": "tslint -c tslint.json 'src/**/*.ts'",
    "build-framework": "node ./build-framework.js"
  },
  "dependencies": {
    "@swindle/core": "^2.1.0",
    "@swindle/os": "^1.0.9"
  },
  "devDependencies": {
    "@types/jest": "^26.0.20",
    "jest": "^26.6.3",
    "ts-jest": "^26.4.4"
  }
}
