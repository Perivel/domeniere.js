import { Id } from "./../id/id";
import { v4 } from 'uuid';
export class UUID extends Id {
    constructor(value) {
        super(value);
    }
    static V4() {
        return new UUID(v4());
    }
    equals(suspect) {
        let isEqual = false;
        if (suspect instanceof UUID) {
            const other = suspect;
            isEqual = this.id() === other.id();
        }
        return isEqual;
    }
    id() {
        return super.id();
    }
}
