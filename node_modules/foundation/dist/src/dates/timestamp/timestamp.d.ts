import 'moment-timezone';
import { TimestampInterface } from "./timestamp.interface";
import { Equatable } from './../../common/common.module';
export declare class Timestamp implements TimestampInterface, Equatable {
    private date;
    constructor(year: number, month: number, date: number, hours?: number, minutes?: number, seconds?: number, ms?: number);
    static FromDate(date: Date): Timestamp;
    static Now(): Timestamp;
    addDays(numDays: number): Timestamp;
    addHours(numHours: number): Timestamp;
    addMinutes(numMinutes: number): Timestamp;
    addMonths(numMonths: number): Timestamp;
    addYears(numYears: number): Timestamp;
    day(): number;
    isAfter(suspect: any): boolean;
    isBefore(suspect: any): boolean;
    equals(suspect: any): boolean;
    month(): number;
    subtractDays(numDays: number): Timestamp;
    subtractHours(numHours: number): Timestamp;
    subtractMinutes(numMinutes: number): Timestamp;
    subtractMonths(numMonths: number): Timestamp;
    subtractYears(numYears: number): Timestamp;
    utcString(): string;
    value(): Date;
    year(): number;
    private toMomentObject;
}
//# sourceMappingURL=timestamp.d.ts.map