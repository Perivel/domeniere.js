import { Timezone } from "./../../geography/geography.module";
export interface DateTimeInterface {
    addDays(numDays: number): DateTimeInterface;
    addHours(numHours: number): DateTimeInterface;
    addMinutes(numMinutes: number): DateTimeInterface;
    addMonths(numMonths: number): DateTimeInterface;
    addYears(numYears: number): DateTimeInterface;
    day(): number;
    isAfter(suspect: any): boolean;
    isBefore(suspect: any): boolean;
    month(): number;
    subtractDays(numDays: number): DateTimeInterface;
    subtractHours(numHours: number): DateTimeInterface;
    subtractMinutes(numMinutes: number): DateTimeInterface;
    subtractMonths(numMonths: number): DateTimeInterface;
    subtractYears(numYears: number): DateTimeInterface;
    timezone(): Timezone;
    toTimezone(timezone: Timezone): DateTimeInterface;
    toUtc(): DateTimeInterface;
    utcString(): string;
    value(): Date;
    year(): number;
}
//# sourceMappingURL=datetime.interface.d.ts.map